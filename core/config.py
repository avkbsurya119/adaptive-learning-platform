"""Global configuration settings for the adaptive learning platform.

This file is optional and reserved for future extensions such as:
- enabling/disabling persistence backends
- logging verbosity
- recommendation weights tuning
"""
CONFIG = {
    "persistence_backend": "json",  # json | sqlite (future)
}
